/** Stores references to every cell in work area*/
export default class CellsStore {
    /** Actual references - two dimensional array(row and column)*/
    static cells: HTMLCanvasElement[][] | null = null;
}
