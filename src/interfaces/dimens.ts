/**
 * Determines the dimens objects, which is used to define cells container div dimensions.
 *
 * */
export default interface Dimens {
    /** width of the container(in pixels) */
    width: number;
    /** height of the container(in pixels) */
    height: number;
}
